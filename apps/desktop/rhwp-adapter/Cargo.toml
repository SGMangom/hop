[package]
name = "hop-rhwp-adapter"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rhwp = { path = "../../../target-local/rhwp-engine" }

[features]
default = []
native-skia = ["rhwp/native-skia"]
